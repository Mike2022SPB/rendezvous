<div class="box-small">
  <% image_info = "#{render partial: 'images/image_info', locals: { image: image }}" %>
  <%= link_to image_tag(image.image.url, size: "150x125"), image.image.url, data: { lightbox: 'image', title: image_info} %>
</div>
