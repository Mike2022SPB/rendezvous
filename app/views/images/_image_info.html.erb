<span class='me-2'>Added by:</span>

<%= render partial: 'users/user', object: image.user %>

<%= button_to 'Delete',
              event_image_path(image.event, image),
              form: { data: { turbo_confirm: "Are you sure?" } },
              method: :delete,
              class: 'btn btn-danger box-destroy' %>
